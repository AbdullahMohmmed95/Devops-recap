pipeline {
    agent any
    triggers {
        // Replace 'genericWebhook' with a valid trigger type from the list
        // For example, if you're using GitHub, you might want to use 'githubPush'
        githubPush()
    }
    stages('Test') {
        // Define your stages here
    }
    // Post-build actions, if any
}
